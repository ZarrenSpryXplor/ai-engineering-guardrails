# Spacelift enterprise source

Generated examples point to the repository's Rego v1 bundles and Spaces/RBAC guidance. They are never deployed automatically. Organisation-specific spaces, labels, identities, resource types, regions, accounts, and thresholds remain configuration data.
