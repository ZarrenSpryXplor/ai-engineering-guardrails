# Codex managed configuration source

Generated examples target the current Codex 0.138+ `requirements.toml` permission-profile and managed-hook schema. Older clients ignore managed permission-profile keys and need the separately documented legacy sandbox-mode form. Endpoint management must distribute an immutable hook runtime and Python interpreter to the configured absolute paths; requirements do not distribute scripts. Test version support and precedence across the fleet.
