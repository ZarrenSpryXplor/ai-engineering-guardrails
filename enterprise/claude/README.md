# Claude Code managed settings source

Generated examples show a managed PreToolUse hook only. An organisation must merge its existing permissions and managed settings, distribute the immutable runtime separately, and avoid global model environment variables that defeat role-specific routing.
