# Python CI-only tooling. Keep exact direct pins aligned across every hosted runner.
# OPA is a pinned binary test dependency installed by the official setup action.
pip==26.2.1
setuptools==84.0.0
build==1.5.0
wheel==0.47.0
twine==7.0.0
rich==15.0.0
